Readline Module:-  it is a module provided by the nodejs for reading the data from readable stream like std.in and std.out 

std.in --> the stream which is useful to read the input 
and std.in for writing the output from it

install Readline npm-i-readline
imported Readline :- const readline = require("readline")
 createInterface({  // created the instance
 input:Std.in
 output: Std.in
 });
 showMenu() ----> For showing the options in it ;
 end();
